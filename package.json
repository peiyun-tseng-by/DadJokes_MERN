{
  "name": "react-test",
  "version": "1.0.0",
  "description": "Dad Jokes App",
  "main": "index.js",
  "scripts": {
    "server": "npm run server --prefix server",
    "client": "npm start --prefix react-test",
    "start": "npm run server",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-server": "npm install --prefix server",
    "package-ui": "npm install --prefix react-test && npm run build --prefix react-test",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm run install-server && npm run package-ui"
  },
  "author": "Pei-Yun",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
